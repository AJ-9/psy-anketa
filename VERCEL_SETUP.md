# 🔧 Настройка базы данных для Vercel

## Проблема
На Vercel используется PostgreSQL, а схема Prisma настроена на SQLite. Это вызывает ошибку "Failed to fetch responses".

## Решение

### Вариант 1: Использовать Vercel Postgres (Рекомендуется)

1. **В Vercel Dashboard:**
   - Перейдите в ваш проект
   - Откройте вкладку **Storage**
   - Нажмите **Create Database** → **Postgres**
   - Создайте базу данных

2. **Скопируйте DATABASE_URL:**
   - После создания БД, Vercel автоматически добавит переменную `DATABASE_URL`
   - Проверьте в **Settings** → **Environment Variables**

3. **Обновите Prisma schema:**
   ```prisma
   datasource db {
     provider = "postgresql"  // Измените с "sqlite" на "postgresql"
     url      = env("DATABASE_URL")
   }
   ```

4. **Примените миграции:**
   ```bash
   npx prisma migrate deploy
   ```

5. **Деплой:**
   ```bash
   git add .
   git commit -m "Update Prisma schema for PostgreSQL"
   git push origin main
   ```

### Вариант 2: Использовать внешний PostgreSQL (Supabase/Neon)

1. **Создайте базу данных:**
   - **Supabase:** https://supabase.com (бесплатно)
   - **Neon:** https://neon.tech (бесплатно)

2. **Скопируйте Connection String:**
   - Формат: `postgresql://user:password@host:port/database?sslmode=require`

3. **Добавьте в Vercel:**
   - **Settings** → **Environment Variables**
   - Добавьте `DATABASE_URL` со значением из шага 2

4. **Обновите Prisma schema** (как в варианте 1)

5. **Примените миграции и деплой**

### Вариант 3: Двойная схема (SQLite локально, PostgreSQL на продакшене)

Создайте два файла схемы:

**`prisma/schema.prisma`** (основной):
```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

**`prisma/schema.sqlite.prisma`** (для локальной разработки):
```prisma
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL") // или "file:./prisma/dev.db"
}
```

Используйте разные схемы:
- Локально: `DATABASE_URL="file:./prisma/dev.db" npx prisma --schema=./prisma/schema.sqlite.prisma migrate dev`
- Продакшен: `npx prisma migrate deploy`

---

## После настройки БД

1. **Проверьте подключение:**
   - Откройте панель психолога на Vercel
   - Должны загрузиться анкеты (если они есть)

2. **Если ошибка сохраняется:**
   - Проверьте логи в Vercel Dashboard → **Deployments** → выберите последний деплой → **Logs**
   - Убедитесь, что `DATABASE_URL` правильно настроен
   - Проверьте, что миграции применены: `npx prisma migrate status`

---

## Быстрая проверка

Выполните в терминале:
```bash
# Проверьте переменные окружения Vercel
vercel env ls

# Или через веб-интерфейс:
# Vercel Dashboard → Settings → Environment Variables
```

---

**После выполнения этих шагов база данных должна работать на Vercel!**
