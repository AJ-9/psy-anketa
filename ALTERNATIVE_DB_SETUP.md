# 🔄 Альтернативная настройка базы данных (без Vercel Dashboard)

Если вы не можете зайти в Vercel Dashboard, используйте внешний PostgreSQL сервис.

## ✅ Вариант 1: Supabase (Бесплатно, 2 минуты)

### Шаг 1: Создайте проект в Supabase

1. Откройте: **https://supabase.com**
2. Нажмите **Start your project** (или **Sign Up**)
3. Войдите через GitHub (или создайте аккаунт)
4. Нажмите **New Project**
5. Заполните:
   - **Name**: `psy-anketa` (или любое имя)
   - **Database Password**: придумайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший (например, `West US`)
6. Нажмите **Create new project**

### Шаг 2: Получите Connection String

1. Дождитесь создания проекта (1-2 минуты)
2. В левом меню выберите **Settings** (шестеренка)
3. Перейдите в **Database**
4. Прокрутите до секции **Connection string**
5. Выберите вкладку **URI**
6. Скопируйте строку подключения (она начинается с `postgresql://`)

### Шаг 3: Добавьте в Vercel через CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Войдите в Vercel
vercel login

# Добавьте переменную окружения
vercel env add DATABASE_URL production

# Когда спросит значение, вставьте скопированную строку из Supabase
# Нажмите Enter

# Также добавьте для preview и development
vercel env add DATABASE_URL preview
vercel env add DATABASE_URL development
```

### Шаг 4: Передеплойте проект

```bash
# Сделайте пустой коммит для триггера деплоя
git commit --allow-empty -m "Trigger redeploy with Supabase DB"
git push origin main
```

---

## ✅ Вариант 2: Neon (Бесплатно, 2 минуты)

### Шаг 1: Создайте проект в Neon

1. Откройте: **https://neon.tech**
2. Нажмите **Sign Up** (можно через GitHub)
3. После входа нажмите **Create Project**
4. Заполните:
   - **Name**: `psy-anketa`
   - **Region**: выберите ближайший
   - **PostgreSQL version**: оставьте по умолчанию
5. Нажмите **Create Project**

### Шаг 2: Получите Connection String

1. После создания проекта вы увидите Connection String
2. Или перейдите в **Dashboard** → выберите проект → **Connection Details**
3. Скопируйте строку подключения

### Шаг 3: Добавьте в Vercel (как в варианте 1, шаг 3)

---

## ✅ Вариант 3: Использовать Vercel CLI для создания БД

Если у вас есть доступ к Vercel CLI, но нет доступа к Dashboard:

```bash
# Войдите в Vercel
vercel login

# Создайте базу данных (если команда доступна)
vercel storage create postgres --name psy-anketa-db

# Или используйте API напрямую
# (требует токен доступа)
```

---

## 🔧 После настройки DATABASE_URL

### Примените миграции:

```bash
# Получите переменные окружения локально
vercel env pull .env.local

# Примените миграции
npx prisma migrate deploy
```

Или миграции применятся автоматически при деплое через `postinstall` скрипт.

---

## ✅ Проверка

После настройки:

1. Подождите завершения деплоя на Vercel
2. Откройте: `https://psy-anketa.vercel.app/admin`
3. Войдите в панель психолога
4. Анкеты должны загрузиться без ошибок

---

## 🆘 Если не можете использовать Vercel CLI

Если у вас нет доступа к Vercel CLI, можно добавить переменную через GitHub Secrets (если используете GitHub Actions) или попросить кого-то с доступом добавить переменную.

---

**Рекомендация: Используйте Supabase - это самый простой и быстрый способ!**

