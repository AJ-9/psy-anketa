# 🤖 Автоматическая настройка Vercel PostgreSQL

К сожалению, я не могу напрямую создать базу данных в Vercel через веб-интерфейс, так как это требует авторизации в вашем аккаунте. Однако я создал скрипт, который поможет автоматизировать процесс после создания БД.

## ⚡ Быстрая настройка (5 минут)

### Шаг 1: Создайте БД в Vercel Dashboard (2 минуты)

**Выполните эти действия вручную:**

1. Откройте: **https://vercel.com/dashboard**
2. Выберите проект: **psy-anketa**
3. Перейдите во вкладку **Storage** (в верхнем меню)
4. Нажмите **Create Database**
5. Выберите **Postgres**
6. Нажмите **Create** (бесплатный план доступен)

**Vercel автоматически:**
- ✅ Создаст PostgreSQL базу данных
- ✅ Добавит переменную `DATABASE_URL` в Environment Variables
- ✅ Подключит БД к проекту

### Шаг 2: Примените миграции (автоматически)

После создания БД, миграции применятся автоматически при следующем деплое через скрипт `postinstall` в `package.json`.

**Или вручную через Vercel CLI:**

```bash
# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Войдите в Vercel
vercel login

# Примените миграции
cd /Users/alikbidzhiev/Documents/Cursor_Psy
vercel env pull .env.local
npx prisma migrate deploy
```

### Шаг 3: Передеплойте проект

Vercel автоматически пересоберет проект после создания БД. Или передеплойте вручную:

1. В Vercel Dashboard → **Deployments**
2. Найдите последний деплой
3. Нажмите **⋯** → **Redeploy**

---

## 🔄 Альтернатива: Используйте скрипт

Я создал скрипт, который поможет проверить настройку:

```bash
chmod +x scripts/setup-vercel-db.sh
./scripts/setup-vercel-db.sh
```

---

## ✅ Проверка

После настройки:

1. Откройте: **https://psy-anketa.vercel.app/admin**
2. Войдите в панель психолога
3. Анкеты должны загрузиться без ошибок

---

## 🆘 Если что-то пошло не так

### Проблема: DATABASE_URL не установлен

**Решение:**
1. Проверьте Vercel Dashboard → Settings → Environment Variables
2. Должна быть переменная `DATABASE_URL`
3. Если её нет, пересоздайте БД через Storage

### Проблема: Миграции не применены

**Решение:**
```bash
# Через Vercel CLI
vercel env pull .env.local
npx prisma migrate deploy

# Или передеплойте проект - миграции применятся автоматически
```

### Проблема: Ошибка подключения

**Решение:**
1. Проверьте логи в Vercel Dashboard → Deployments → Logs
2. Убедитесь, что БД создана и подключена
3. Проверьте формат DATABASE_URL (должен начинаться с `postgresql://`)

---

## 📝 Что я сделал автоматически

✅ Обновил Prisma schema для PostgreSQL  
✅ Добавил проверку DATABASE_URL в API  
✅ Улучшил обработку ошибок  
✅ Добавил автоматическое применение миграций через `postinstall`  

**Осталось только создать БД в Vercel Dashboard (шаг 1) - это займет 2 минуты!**

