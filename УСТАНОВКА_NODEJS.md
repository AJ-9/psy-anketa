# 📦 Установка Node.js

Node.js не установлен на вашем Mac. Есть 2 способа установки:

## Способ 1: Скачать установщик (САМЫЙ ПРОСТОЙ) ⭐

1. Откройте в браузере: **https://nodejs.org/**
2. Нажмите кнопку **"Download Node.js (LTS)"** (зеленая кнопка)
3. Скачайте установщик для macOS (`.pkg` файл)
4. Запустите скачанный файл и следуйте инструкциям установщика
5. После установки **перезапустите терминал** (закройте и откройте заново)
6. Проверьте установку:
   ```bash
   node --version
   npm --version
   ```

## Способ 2: Установить через Homebrew

### Шаг 1: Установите Homebrew

Выполните в терминале:
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

Следуйте инструкциям на экране. Может потребоваться ввод пароля.

### Шаг 2: Установите Node.js

```bash
brew install node
```

### Шаг 3: Проверьте установку

```bash
node --version  # должна быть версия 18 или выше
npm --version   # должна быть версия 9 или выше
```

---

## После установки Node.js:

Выполните в терминале:

```bash
cd /Users/alikbidzhiev/Documents/Cursor_Psy
npm install
npx prisma generate
npx prisma migrate dev --name init
npm run dev
```

---

## Рекомендация:

**Используйте Способ 1** - он проще и быстрее. Просто скачайте установщик с nodejs.org и установите его.

После установки **обязательно перезапустите терминал**!

