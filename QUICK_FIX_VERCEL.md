# 🚨 Быстрое исправление ошибки "Failed to fetch responses" на Vercel

## Проблема
Ошибка "Failed to fetch responses" означает, что база данных не настроена на Vercel.

## ⚠️ Если не можете зайти в Vercel Dashboard

Используйте **альтернативный способ** - внешний PostgreSQL:
- **Supabase** (бесплатно): https://supabase.com
- **Neon** (бесплатно): https://neon.tech

Подробная инструкция: см. файл `ALTERNATIVE_DB_SETUP.md`

---

## ✅ Решение за 3 шага (через Vercel Dashboard):

### Шаг 1: Создайте PostgreSQL базу данных (2 минуты)

1. Откройте: **https://vercel.com/dashboard**
2. Выберите проект **psy-anketa**
3. Перейдите во вкладку **Storage** (в верхнем меню)
4. Нажмите **Create Database**
5. Выберите **Postgres**
6. Нажмите **Create** (бесплатный план доступен)

### Шаг 2: Проверьте переменные окружения (30 секунд)

1. В том же проекте перейдите в **Settings**
2. Откройте **Environment Variables**
3. Убедитесь, что есть переменная **`DATABASE_URL`**
   - Если её нет, Vercel добавит её автоматически после создания БД
   - Формат: `postgresql://user:password@host:port/database?sslmode=require`

### Шаг 3: Передеплойте проект (1 минута)

1. В Vercel Dashboard перейдите в **Deployments**
2. Найдите последний деплой (должен быть с коммитом про PostgreSQL)
3. Нажмите **⋯** (три точки) → **Redeploy**
4. Или просто сделайте новый push:
   ```bash
   git commit --allow-empty -m "Trigger redeploy"
   git push origin main
   ```

---

## ✅ Проверка

После передеплоя:

1. Откройте панель психолога: `https://psy-anketa.vercel.app/admin`
2. Войдите (если требуется)
3. Анкеты должны загрузиться без ошибок

---

## 🔍 Если ошибка сохраняется:

### Проверьте логи:

1. В Vercel Dashboard → **Deployments**
2. Выберите последний деплой
3. Откройте вкладку **Logs**
4. Найдите ошибки, связанные с `DATABASE_URL` или `prisma`

### Возможные проблемы:

1. **DATABASE_URL не установлен:**
   - Решение: Создайте БД через Storage (см. Шаг 1)

2. **Миграции не применены:**
   - Решение: Они применятся автоматически при деплое через `postinstall` скрипт

3. **Неверный формат DATABASE_URL:**
   - Решение: Удалите и пересоздайте БД через Storage

---

## 📝 Альтернатива: Использовать внешний PostgreSQL

Если Vercel Postgres не работает, используйте:

### Supabase (бесплатно):
1. Зарегистрируйтесь: https://supabase.com
2. Создайте проект
3. Скопируйте Connection String из Settings → Database
4. Добавьте в Vercel: Settings → Environment Variables → `DATABASE_URL`

### Neon (бесплатно):
1. Зарегистрируйтесь: https://neon.tech
2. Создайте проект
3. Скопируйте Connection String
4. Добавьте в Vercel: Settings → Environment Variables → `DATABASE_URL`

---

## 🎯 После настройки БД:

Проект автоматически:
- ✅ Подключится к PostgreSQL
- ✅ Применит миграции
- ✅ Начнет сохранять и загружать анкеты

**Готово! Ошибка должна исчезнуть.**

