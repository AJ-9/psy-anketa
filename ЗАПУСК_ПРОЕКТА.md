# 🚀 Запуск проекта

Ошибка `ERR_CONNECTION_REFUSED` означает, что сервер не запущен.

## Шаг 1: Установите Node.js (если еще не установлен)

**macOS:**
```bash
# Через Homebrew
brew install node

# Или скачайте с https://nodejs.org/
```

**Проверка:**
```bash
node --version  # должна быть версия 18+
npm --version   # должна быть версия 9+
```

## Шаг 2: Установите зависимости

```bash
cd /Users/alikbidzhiev/Documents/Cursor_Psy
npm install
```

Это займет несколько минут. Дождитесь завершения.

## Шаг 3: Настройте базу данных

```bash
npx prisma generate
npx prisma migrate dev --name init
```

## Шаг 4: Запустите сервер

```bash
npm run dev
```

Вы увидите:
```
  ▲ Next.js 14.x.x
  - Local:        http://localhost:3000
```

## Шаг 5: Откройте в браузере

Откройте: **http://localhost:3000**

---

## Если порт 3000 занят:

```bash
# Используйте другой порт
PORT=3001 npm run dev
```

Затем откройте: http://localhost:3001

## Если есть ошибки:

1. **Ошибка с Prisma:**
```bash
rm -f prisma/dev.db
npx prisma migrate dev --name init
```

2. **Ошибка с зависимостями:**
```bash
rm -rf node_modules package-lock.json
npm install
```

3. **Проверьте версию Node.js:**
```bash
node --version  # должна быть 18 или выше
```

---

**После выполнения всех шагов проект должен запуститься!**

