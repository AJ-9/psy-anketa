# 🔑 Как заменить [YOUR_PASSWORD] в Supabase Connection String

## Проблема
В Connection String показан `[YOUR_PASSWORD]` - это плейсхолдер, который нужно заменить на реальный пароль.

## ✅ Решение: Найдите или сбросьте пароль

### Вариант 1: Найдите пароль в настройках

1. В Supabase Dashboard откройте ваш проект
2. Перейдите в **Settings** (шестеренка внизу слева)
3. Выберите **Database** в меню слева
4. Найдите секцию **Database password**
5. Если пароль скрыт, нажмите **Show** или **Reveal**
6. Скопируйте пароль

### Вариант 2: Сбросьте пароль (если не помните)

1. В **Settings** → **Database**
2. Найдите кнопку **Reset database password** или **Change database password**
3. Нажмите на неё
4. Введите новый пароль (запомните его!)
5. Подтвердите сброс
6. После сброса пароль будет показан - скопируйте его

---

## После получения пароля

### Шаг 1: Замените [YOUR_PASSWORD] в Connection String

**Было:**
```
postgresql://postgres:[YOUR_PASSWORD]@db.zjyvnudhrbldcsakxlyr.supabase.co:5432/postgres
```

**Стало (пример, если пароль "mypassword123"):**
```
postgresql://postgres:mypassword123@db.zjyvnudhrbldcsakxlyr.supabase.co:5432/postgres
```

**Важно:** 
- Если пароль содержит специальные символы (@, #, $, %, &, и т.д.), их нужно URL-encode:
  - `@` → `%40`
  - `#` → `%23`
  - `$` → `%24`
  - `%` → `%25`
  - `&` → `%26`
  - пробел → `%20`

### Шаг 2: Добавьте в Vercel

```bash
# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Войдите в Vercel
vercel login

# Добавьте переменную (вставьте полный Connection String с паролем)
vercel env add DATABASE_URL production
# Вставьте: postgresql://postgres:ВАШ_ПАРОЛЬ@db.zjyvnudhrbldcsakxlyr.supabase.co:5432/postgres
# Нажмите Enter

# Также для preview и development
vercel env add DATABASE_URL preview
vercel env add DATABASE_URL development
```

---

## Или используйте автоматический скрипт

```bash
./scripts/add-database-url.sh
```

Скрипт попросит ввести полный Connection String с паролем.

---

## Пример с URL-encoding

Если ваш пароль: `MyP@ssw0rd#123`

То в Connection String должно быть:
```
postgresql://postgres:MyP%40ssw0rd%23123@db.zjyvnudhrbldcsakxlyr.supabase.co:5432/postgres
```

(где `@` заменен на `%40`, а `#` на `%23`)

---

## После добавления DATABASE_URL

Передеплойте проект:

```bash
git commit --allow-empty -m "Trigger redeploy with Supabase DB"
git push origin main
```

Или передеплойте вручную в Vercel Dashboard.

---

**После этого ошибка должна исчезнуть!**

