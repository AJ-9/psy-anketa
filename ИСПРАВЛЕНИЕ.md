# 🔧 Исправление проблемы

Ошибка `next: command not found` означает, что зависимости не установились.

## Выполните в терминале:

```bash
cd /Users/alikbidzhiev/Documents/Cursor_Psy

# Очистите и переустановите зависимости
rm -rf node_modules package-lock.json
npm install

# Настройте БД
npx prisma generate
npx prisma migrate dev --name init

# Запустите сервер
npm run dev
```

---

## Если npm install выдает ошибки:

1. **Проверьте версию Node.js:**
   ```bash
   node --version  # должна быть 18+
   ```

2. **Очистите кэш npm:**
   ```bash
   npm cache clean --force
   npm install
   ```

3. **Попробуйте с флагом --legacy-peer-deps:**
   ```bash
   npm install --legacy-peer-deps
   ```

---

**После успешной установки запустите `npm run dev`**

