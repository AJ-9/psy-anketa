# 🚀 Настройка Neon PostgreSQL (бесплатно, 3 минуты)

Neon - это бесплатный PostgreSQL сервис, который идеально подходит для продакшена.

## ✅ Шаг 1: Создайте проект в Neon (2 минуты)

1. Откройте: **https://neon.tech**
2. Нажмите **Sign Up** (можно через GitHub)
3. После входа нажмите **Create Project**
4. Заполните форму:
   - **Name**: `psy-anketa` (или любое имя)
   - **Region**: выберите ближайший (например, `US East`)
   - **PostgreSQL version**: оставьте по умолчанию (15)
5. Нажмите **Create Project**

## ✅ Шаг 2: Скопируйте Connection String (30 секунд)

### Где найти Connection String:

**Способ 1 (самый простой):**
- После создания проекта Connection String будет показан **сразу на экране**
- Вы увидите большой блок с текстом, начинающимся с `postgresql://`
- Рядом будет кнопка **Copy** - нажмите на неё

**Способ 2 (если не видите сразу):**
- В Neon Dashboard выберите ваш проект **psy-anketa**
- Нажмите на название проекта
- Найдите вкладку **Connection Details** или **Connect** в верхней части
- Скопируйте строку подключения (она начинается с `postgresql://`)

**Способ 3:**
- В Dashboard проекта → **Settings** → **Connection Details**
- Скопируйте Connection String

**Пример Connection String:**
```
postgresql://user:password@ep-xxx-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require
```

**Важно:** Connection String должен содержать реальный пароль (не `[YOUR_PASSWORD]`)

📖 Подробная инструкция: см. файл `WHERE_TO_FIND_CONNECTION_STRING.md`

## ✅ Шаг 3: Добавьте в Vercel через CLI (1 минута)

### Вариант A: Автоматический скрипт (рекомендуется)

```bash
./scripts/setup-neon.sh
```

Скрипт попросит вставить Connection String и автоматически добавит его в Vercel.

### Вариант B: Вручную

```bash
# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Войдите в Vercel
vercel login

# Добавьте переменную (вставьте Connection String из Neon)
vercel env add DATABASE_URL production
# Когда спросит значение, вставьте Connection String из Neon
# Нажмите Enter

# Также для preview и development
vercel env add DATABASE_URL preview
vercel env add DATABASE_URL development
```

## ✅ Шаг 4: Примените миграции

Миграции применятся автоматически при деплое через `postinstall` скрипт.

Или вручную:

```bash
# Получите переменные окружения локально
vercel env pull .env.local

# Примените миграции
npx prisma migrate deploy
```

## ✅ Шаг 5: Передеплойте проект

```bash
git commit --allow-empty -m "Trigger redeploy with Neon DB"
git push origin main
```

Или передеплойте вручную в Vercel Dashboard.

---

## ✅ Проверка

После деплоя:

1. Подождите завершения деплоя (1-2 минуты)
2. Откройте: `https://psy-anketa.vercel.app/admin`
3. Войдите в панель психолога
4. Анкеты должны загрузиться без ошибок!

---

## 🎯 Преимущества Neon

- ✅ **Бесплатно** - есть бесплатный план
- ✅ **Просто** - Connection String показывается сразу с паролем
- ✅ **Надежно** - полноценный PostgreSQL
- ✅ **Быстро** - создание проекта занимает 1 минуту

---

## 🆘 Если что-то пошло не так

### Проблема: Не могу найти Connection String

**Решение:**
1. В Neon Dashboard выберите ваш проект
2. Перейдите в **Connection Details**
3. Скопируйте строку из секции **Connection string**

### Проблема: Ошибка при добавлении в Vercel

**Решение:**
1. Убедитесь, что вы авторизованы: `vercel login`
2. Проверьте формат Connection String (должен начинаться с `postgresql://`)
3. Убедитесь, что пароль в строке не содержит специальных символов без URL-encoding

---

**Готово! После выполнения этих шагов база данных будет работать на Vercel!**

