# 📍 Где найти Connection String в Neon

## Способ 1: Сразу после создания проекта (самый простой)

После того как вы нажмете **Create Project** в Neon:

1. **На экране создания проекта** Connection String будет показан сразу
2. Вы увидите большой блок с текстом, начинающимся с `postgresql://`
3. Рядом будет кнопка **Copy** - нажмите на неё
4. Connection String скопируется в буфер обмена

**Это самый простой способ!**

---

## Способ 2: В Dashboard проекта

Если вы уже создали проект и не видите Connection String:

1. В Neon Dashboard найдите ваш проект **psy-anketa**
2. Нажмите на название проекта, чтобы открыть его
3. В верхней части страницы найдите вкладку **Connection Details** или **Connect**
4. Нажмите на неё
5. Вы увидите секцию **Connection string**
6. Скопируйте строку (она начинается с `postgresql://`)

---

## Способ 3: В настройках проекта

1. В Neon Dashboard выберите ваш проект
2. В левом меню найдите **Settings** (шестеренка)
3. Перейдите в **Connection Details**
4. Найдите секцию **Connection string**
5. Скопируйте строку

---

## Как выглядит Connection String

Connection String выглядит примерно так:

```
postgresql://username:password@ep-xxx-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require
```

**Важно:** 
- Строка должна начинаться с `postgresql://`
- Должна содержать пароль (не `[YOUR_PASSWORD]`)
- Должна заканчиваться на `?sslmode=require` или похожее

---

## Если не можете найти Connection String

1. **Проверьте, что проект создан:**
   - В Neon Dashboard должен быть ваш проект
   - Статус проекта должен быть "Active" или "Ready"

2. **Попробуйте пересоздать проект:**
   - Создайте новый проект
   - Connection String будет показан сразу после создания

3. **Используйте альтернативный способ:**
   - В Dashboard проекта найдите кнопку **Connect** или **Connection Details**
   - Там должен быть Connection String

---

## После копирования Connection String

Используйте автоматический скрипт для добавления в Vercel:

```bash
./scripts/setup-neon.sh
```

Или добавьте вручную через Vercel CLI (см. инструкцию в `SETUP_NEON.md`).

---

**Самый простой способ: Connection String показывается сразу после создания проекта в Neon!**

