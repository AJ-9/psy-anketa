# 🚀 Запуск проекта вручную

Если автоматический запуск не сработал, выполните команды в терминале:

## Шаг 1: Откройте терминал

В Cursor: `Terminal` → `New Terminal`
Или используйте системный терминал macOS

## Шаг 2: Перейдите в директорию проекта

```bash
cd /Users/alikbidzhiev/Documents/Cursor_Psy
```

## Шаг 3: Установите зависимости (если еще не установлены)

```bash
npm install
```

Дождитесь завершения (может занять 1-2 минуты).

## Шаг 4: Настройте базу данных

```bash
npx prisma generate
npx prisma migrate dev --name init
```

## Шаг 5: Запустите сервер

```bash
npm run dev
```

Вы увидите:
```
  ▲ Next.js 14.x.x
  - Local:        http://localhost:3000
  - ready started server on 0.0.0.0:3000
```

## Шаг 6: Откройте браузер

Откройте: **http://localhost:3000**

---

## Если Node.js не установлен:

```bash
# Через Homebrew
brew install node

# Или скачайте с https://nodejs.org/
```

Проверка:
```bash
node --version  # должна быть 18+
```

## Если есть ошибки:

1. **Ошибка "command not found: npm"**
   - Установите Node.js (см. выше)

2. **Ошибка с Prisma**
   ```bash
   rm -f prisma/dev.db
   npx prisma migrate dev --name init
   ```

3. **Порт 3000 занят**
   ```bash
   PORT=3001 npm run dev
   ```
   Затем откройте: http://localhost:3001

---

**Важно:** Сервер должен оставаться запущенным в терминале. Не закрывайте терминал!

Для остановки нажмите `Ctrl + C`.

