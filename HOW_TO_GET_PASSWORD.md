# 🔑 Как найти пароль базы данных в Supabase

## Где найти пароль

### Вариант 1: В настройках проекта

1. В Supabase Dashboard откройте ваш проект
2. Перейдите в **Settings** (шестеренка внизу слева)
3. Выберите **Database** в меню слева
4. Найдите секцию **Database password**
5. Если пароль скрыт, нажмите **Show** или **Reveal**

### Вариант 2: Если забыли пароль

1. В **Settings** → **Database**
2. Найдите кнопку **Reset database password**
3. Нажмите на неё
4. Введите новый пароль (сохраните его!)
5. Подтвердите сброс

### Вариант 3: При создании проекта

Если вы только что создали проект, пароль был указан при создании. Проверьте, где вы его сохранили.

---

## Как использовать пароль

После того как найдете пароль, замените `[YOUR_PASSWORD]` в Connection String:

**Было:**
```
postgresql://postgres:[YOUR_PASSWORD]@db.zjyvnudhrbldcsakxlyr.supabase.co:5432/postgres
```

**Стало (пример):**
```
postgresql://postgres:your_actual_password_here@db.zjyvnudhrbldcsakxlyr.supabase.co:5432/postgres
```

**Важно:** Не используйте пробелы или специальные символы, которые могут сломать URL. Если пароль содержит специальные символы, они должны быть URL-encoded.

---

## Быстрый способ через скрипт

Я создал скрипт, который поможет добавить Connection String в Vercel:

```bash
./scripts/add-database-url.sh
```

Скрипт попросит ввести полный Connection String с паролем и автоматически добавит его в Vercel.

---

## Или вручную через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Войдите в Vercel
vercel login

# Добавьте переменную (вставьте полный Connection String с паролем)
vercel env add DATABASE_URL production
# Вставьте: postgresql://postgres:ВАШ_ПАРОЛЬ@db.zjyvnudhrbldcsakxlyr.supabase.co:5432/postgres

# Также для preview и development
vercel env add DATABASE_URL preview
vercel env add DATABASE_URL development
```

---

**После добавления DATABASE_URL передеплойте проект, и всё заработает!**

