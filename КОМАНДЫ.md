# ✅ ВЫПОЛНИТЕ ЭТИ КОМАНДЫ

## Важно: сначала перейдите в директорию проекта!

```bash
cd /Users/alikbidzhiev/Documents/Cursor_Psy
```

## Затем выполните:

```bash
git add lib/pdfExport.ts app/admin/page.tsx
git commit -m "Fix TypeScript errors"
git push
```

---

**Или все одной строкой:**

```bash
cd /Users/alikbidzhiev/Documents/Cursor_Psy && git add lib/pdfExport.ts app/admin/page.tsx && git commit -m "Fix TypeScript errors" && git push
```

---

**Вы находитесь в домашней директории `~`, нужно перейти в директорию проекта!**

